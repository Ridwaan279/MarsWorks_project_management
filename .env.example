# Postgres connection string. Locally this can be a docker container; in
# production use the Supabase "Session pooler" URI (port 5432/6543).
DATABASE_URL="postgresql://user:password@host:5432/marsworks?schema=public"
